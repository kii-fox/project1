# Project1

## Overview
Flask application for practice.

## Tech Stack
- Python
- Flask
- Git

## Setup
```bash
python -m venv venv  
pip install -r requirements.txt  
git commit -m "initial project setup"git commit -m "Update README"
